# Google Generative AI Configuration
# Get your free API key from: https://aistudio.google.com/app/apikeys
# Once you have your API key, create a .env file and add this line:

VITE_GOOGLE_GEMINI_API_KEY=your_api_key_here

# The app will automatically use Google Gemini AI for enhanced summaries
# If the API key is not configured, the app will use the local AI algorithm
